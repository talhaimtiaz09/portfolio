import type { Metadata } from "next";
// Remove unused Geist imports
import { Space_Grotesk,Great_Vibes } from "next/font/google"; 
import "./globals.css";
import SmoothScroll from "@/components/SmoothScroll";

// Use the full object correctly
const grotesk_font = Space_Grotesk({
  variable: "--font-space-grotesk",
  subsets: ["latin"],
});

const greatVibes = Great_Vibes({
  subsets: ['latin'],
  weight: ['400'], // Great Vibes only has a 400 weight
  display: 'swap',
  variable: '--font-great-vibes', // Optional: for using with CSS variables
});
export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    // Correctly apply the generated class name using .className
    <html lang="en">
      <body
        className={`${grotesk_font.className} antialiased`}
      >
<SmoothScroll>
          {children}
        </SmoothScroll>
      </body>
    </html>
  );
}